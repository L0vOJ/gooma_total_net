{
  "name": "gooma_total_net",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "build:react":"cd ./packages/reacter && yarn build",
    "copy:build":"cp -r ./packages/reacter/build ./packages/gate/.keystone/public",
    "build:react:full":"yarn build:react && yarn copy:build", 
    "dev": "yarn workspaces run dev"
  }
}
